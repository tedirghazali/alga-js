import { 
  insert, 
  toggle, 
  flatten, 
  nested, 
  index, 
  search, 
  searchBy, 
  filtered, 
  sort, 
  sorted,
  paginate, 
  pages, 
  show, 
  pagination,
  sum,
  unique
} from './arrayLib.js'

export default {
  insert,
  toggle,
  flatten,
  nested,
  index,
  search, 
  searchBy, 
  filtered,
  sort,
  sorted,
  paginate, 
  pages, 
  show, 
  pagination,
  sum,
  unique
}
