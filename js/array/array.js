import { 
  insert,
  update,
  destroy,
  select,
  hidden,
  toggle, 
  flatten, 
  nested, 
  index, 
  search, 
  searchBy, 
  filtered, 
  sort, 
  sorted,
  paginate, 
  pages, 
  show, 
  pagination,
  sum,
  unique
} from './arrayLib.js'

export default {
  insert,
  update,
  destroy,
  select,
  hidden,
  toggle,
  flatten,
  nested,
  index,
  search, 
  searchBy, 
  filtered,
  sort,
  sorted,
  paginate, 
  pages, 
  show, 
  pagination,
  sum,
  unique
}
