import { 
  insert, 
  toggle, 
  flatten, 
  nested, 
  index, 
  search, 
  filtered, 
  sort, 
  sorted,
  paginate, 
  pages, 
  show, 
  pagination
} from './arrayLib.js'

export default {
  insert,
  toggle,
  flatten,
  nested,
  index,
  search,
  filtered,
  sort,
  sorted,
  paginate, 
  pages, 
  show, 
  pagination
}
