import { setCookie, getCookie, hasCookie, removeCookie, clearCookie } from './cookieStorage.js'
import { isStorage, isCookie } from './isStorage.js'
import { setStorage, getStorage, hasStorage, removeStorage, clearStorage } from './itemStorage.js'

export { 
  setCookie,
  getCookie,
  hasCookie,
  removeCookie,
  clearCookie ,
  isStorage,
  isCookie,
  setStorage,
  getStorage,
  hasStorage,
  removeStorage,
  clearStorage
}
